window.addEventListener('load', () => {
    let top_bar_elmt_logo_image = document.querySelector('#top_bar_logo_image');

    top_bar_elmt_logo_image.setAttribute('src', `${$_base_url}assets/imagens/cybers_logo.svg`);
});